#!/bin/bash

wifi=$( ifconfig | grep -A 1 "wlan" | sed 'N;s/\n/ /;N;s/\n/ /' | awk '{ print $7}' | cut -d: -f2 )

if [ -z "$wifi" ]
then
echo "Geen ip gevonden, zit deze aangesloten?"
else
echo "$wifi"
fi

